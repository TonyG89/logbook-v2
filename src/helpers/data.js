// const getAmount = (work, filteredValue = '', data) => {
//   let amount
//   if (!filteredValue) amount = data?.reduce((sum, item) => sum + +item[work], 0)
//   else {
//     const filteredObj = data.filter(obj => obj.status === filteredValue)
//     amount = filteredObj?.reduce((sum, item) => sum + +item[work], 0)
//   }
//   return amount
// }

// export { getAmount }